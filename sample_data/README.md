# Sample Materials Dataset

`materials_experiment_sample.csv` is a small synthetic dataset you can use to test the app immediately.

It includes:

- `sample_id`
- `batch`
- `material`
- `condition`
- `replicate`
- multiple numeric measurements

It is intentionally simple and is meant for testing the full workflow before connecting real lab data.
